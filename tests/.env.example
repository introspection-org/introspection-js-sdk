# Replay (the default mode) needs no credentials — the committed cassettes
# carry the provider responses. These are only for POLLY_MODE=record.
ANTHROPIC_API_KEY=sk-ant-...
INTROSPECTION_TOKEN=your-introspection-token
